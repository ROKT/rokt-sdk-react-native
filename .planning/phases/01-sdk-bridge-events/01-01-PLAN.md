---
phase: 01-sdk-bridge-events
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Rokt.Widget/src/NativeRoktWidget.ts
  - Rokt.Widget/src/Rokt.tsx
  - Rokt.Widget/android/build.gradle
  - Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt
autonomous: true
requirements:
  [
    SDK-02,
    REN-01,
    REN-04,
    REN-05,
    REM-01,
    REM-02,
    REM-03,
    REM-04,
    EVT-02,
    SHP-01,
    SHP-04,
  ]

must_haves:
  truths:
    - "TurboModule spec has selectPlacements/selectPlacementsWithConfig (not execute)"
    - "TurboModule spec has selectShoppableAds/selectShoppableAdsWithConfig"
    - "TurboModule spec does NOT have execute, execute2Step, setLoggingEnabled, setFulfillmentAttributes, setSessionId, getSessionId"
    - "Rokt.tsx has selectPlacements and selectShoppableAds as public static methods"
    - "Rokt.tsx does NOT have execute, execute2Step, setLoggingEnabled, setFulfillmentAttributes, setSessionId, getSessionId"
    - "Android build.gradle references roktsdk:5.0.0"
    - "Android RNRoktWidgetModuleImpl uses selectPlacements (not execute) and unified onEvent flow"
    - "Android RNRoktWidgetModuleImpl does NOT have setLoggingEnabled, setFulfillmentAttributes, setSessionId, getSessionId, createRoktCallback"
  artifacts:
    - path: "Rokt.Widget/src/NativeRoktWidget.ts"
      provides: "TurboModule spec with v5.0.0 API surface"
      contains: "selectPlacements"
    - path: "Rokt.Widget/src/Rokt.tsx"
      provides: "Public API with selectPlacements and selectShoppableAds"
      contains: "selectPlacements"
    - path: "Rokt.Widget/android/build.gradle"
      provides: "Android SDK dependency at 5.0.0"
      contains: "roktsdk:5.0.0"
    - path: "Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt"
      provides: "Android shared implementation with v5.0.0 APIs"
      contains: "selectPlacements"
  key_links:
    - from: "Rokt.tsx"
      to: "NativeRoktWidget.ts"
      via: "method calls through RNRoktWidget"
      pattern: "RNRoktWidget\\.selectPlacements"
    - from: "RNRoktWidgetModuleImpl.kt"
      to: "Rokt SDK 5.0.0"
      via: "Rokt.selectPlacements call"
      pattern: "Rokt\\.selectPlacements"
---

<objective>
Rewrite TypeScript layer and Android shared implementation for SDK 5.0.0 API surface.

Purpose: Apply teammate's baseline from feat/remove-deprecated-functions for TypeScript and Android layers, then layer shoppable ads on top. This gives us the renamed APIs, removed deprecated methods, and new event pattern in one coherent pass.

Output: Clean NativeRoktWidget.ts spec, updated Rokt.tsx public API, Android build.gradle with SDK 5.0.0, and Android RNRoktWidgetModuleImpl.kt matching teammate's cleaned-up version with shoppable-ads event handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sdk-bridge-events/01-CONTEXT.md

Key baseline: Fetch teammate files via:
git show origin/feat/remove-deprecated-functions:Rokt.Widget/src/NativeRoktWidget.ts
git show origin/feat/remove-deprecated-functions:Rokt.Widget/src/Rokt.tsx
git show origin/feat/remove-deprecated-functions:Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt
git show origin/feat/remove-deprecated-functions:Rokt.Widget/android/build.gradle

<interfaces>
<!-- Teammate's NativeRoktWidget.ts spec (the target shape, minus shoppable ads) -->
interface Spec extends TurboModule {
  initialize(roktTagId: string, appVersion: string): void;
  initializeWithFontFiles(roktTagId: string, appVersion: string, fontsMap: { [key: string]: string }): void;
  selectPlacements(identifier: string, attributes: { [key: string]: string }, placeholders: { [key: string]: number | null }): void;
  selectPlacementsWithConfig(identifier: string, attributes: { [key: string]: string }, placeholders: { [key: string]: number | null }, roktConfig: RoktConfigType): void;
  purchaseFinalized(placementId: string, catalogItemId: string, success: boolean): void;
  setEnvironmentToStage(): void;
  setEnvironmentToProd(): void;
}

<!-- We ADD to this: -->

selectShoppableAds(identifier: string, attributes: { [key: string]: string }): void;
selectShoppableAdsWithConfig(identifier: string, attributes: { [key: string]: string }, roktConfig: RoktConfigType): void;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Android shared impl and bump SDK to 5.0.0</name>
  <files>
    Rokt.Widget/android/build.gradle,
    Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt
  </files>
  <read_first>
    - git show origin/feat/remove-deprecated-functions:Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt
    - git show origin/feat/remove-deprecated-functions:Rokt.Widget/android/build.gradle
    - Rokt.Widget/android/build.gradle (current, to find the roktsdk version line)
    - Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt (current)
  </read_first>
  <action>
    1. In build.gradle, change `implementation ('com.rokt:roktsdk:4.14.1')` to `implementation ('com.rokt:roktsdk:5.0.0')` (per SDK-02).

    2. Replace RNRoktWidgetModuleImpl.kt with the teammate's version from feat/remove-deprecated-functions. The teammate's version already:
       - Uses `Rokt.selectPlacements` instead of `Rokt.execute` (per REN-03, REN-05)
       - Removed setLoggingEnabled, setFulfillmentAttributes, setSessionId/getSessionId (per REM-01, REM-02, REM-03, REM-04)
       - Removed createRoktCallback and the old RoktCallback/RoktEventCallback patterns
       - Removed roktEventHandler and fulfillmentAttributesCallback fields
       - Removed listeners map
       - Uses unified flow-based event collection via startRoktEventListener

    3. After applying teammate's baseline, add the following shoppable ads event types to the `startRoktEventListener` flow collector `when` block, BEFORE the `else` branch:

       ```kotlin
       is RoktEvent.CartItemInstantPurchaseInitiated -> {
           eventName = "CartItemInstantPurchaseInitiated"
           params.putString("catalogItemId", event.catalogItemId)
           params.putString("cartItemId", event.cartItemId)
           event.identifier
       }

       is RoktEvent.CartItemInstantPurchaseFailure -> {
           eventName = "CartItemInstantPurchaseFailure"
           params.putString("error", event.error)
           params.putString("catalogItemId", event.catalogItemId)
           event.identifier
       }

       is RoktEvent.CartItemDevicePay -> {
           eventName = "CartItemDevicePay"
           params.putString("paymentProvider", event.paymentProvider)
           event.identifier
       }

       is RoktEvent.InstantPurchaseDismissal -> {
           eventName = "InstantPurchaseDismissal"
           event.identifier
       }
       ```

       Note: CartItemInstantPurchase is already handled in teammate's code. The above are the NEW event types from EVT-03.

    4. Verify the teammate's version uses `event.id` for existing events (PlacementReady, etc.) -- this matches SDK 5.0.0's `identifier` property on RoktEvent subclasses. The Kotlin property is `id` not `identifier` on the event classes per the teammate's code.

    IMPORTANT: Do NOT add selectShoppableAds method to this file yet -- that goes in Plan 03 Task 1 when the new/old arch wrappers are also updated.

  </action>
  <acceptance_criteria>
    - grep "roktsdk:5.0.0" Rokt.Widget/android/build.gradle returns a match
    - grep "setLoggingEnabled" Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt returns NO match
    - grep "setFulfillmentAttributes" Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt returns NO match
    - grep "setSessionId" Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt returns NO match
    - grep "getSessionId" Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt returns NO match
    - grep "createRoktCallback" Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt returns NO match
    - grep "Rokt.execute" Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt returns NO match (no old execute calls)
    - grep "CartItemInstantPurchaseInitiated" Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt returns a match
    - grep "CartItemInstantPurchaseFailure" Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt returns a match
    - grep "CartItemDevicePay" Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt returns a match
    - grep "InstantPurchaseDismissal" Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt returns a match
  </acceptance_criteria>
  <verify>
    <automated>cd Rokt.Widget && grep -c "roktsdk:5.0.0" android/build.gradle && ! grep -q "setLoggingEnabled\|setFulfillmentAttributes\|setSessionId\|getSessionId\|createRoktCallback" android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt && grep -q "CartItemInstantPurchaseInitiated" android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt && grep -q "InstantPurchaseDismissal" android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Android build.gradle uses roktsdk:5.0.0. RNRoktWidgetModuleImpl.kt matches teammate baseline with deprecated methods removed, unified event flow, and new shoppable ads events added.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite TurboModule spec and Rokt.tsx public API for v5.0.0</name>
  <files>
    Rokt.Widget/src/NativeRoktWidget.ts,
    Rokt.Widget/src/Rokt.tsx
  </files>
  <read_first>
    - git show origin/feat/remove-deprecated-functions:Rokt.Widget/src/NativeRoktWidget.ts
    - git show origin/feat/remove-deprecated-functions:Rokt.Widget/src/Rokt.tsx
    - Rokt.Widget/src/NativeRoktWidget.ts (current -- has shoppable ads additions we want to keep)
    - Rokt.Widget/src/Rokt.tsx (current -- has selectShoppableAds we want to keep)
  </read_first>
  <action>
    1. **NativeRoktWidget.ts** (per REN-01, REM-01, REM-02, REM-03, REM-04, SHP-01):
       Start from teammate's version which already has selectPlacements/selectPlacementsWithConfig and removed all deprecated methods. Then add back the shoppable ads methods from our current branch:

       Final spec should contain EXACTLY these methods:
       - initialize(roktTagId, appVersion): void
       - initializeWithFontFiles(roktTagId, appVersion, fontsMap): void
       - selectPlacements(identifier, attributes, placeholders): void
       - selectPlacementsWithConfig(identifier, attributes, placeholders, roktConfig): void
       - selectShoppableAds(identifier, attributes): void
       - selectShoppableAdsWithConfig(identifier, attributes, roktConfig): void
       - purchaseFinalized(placementId, catalogItemId, success): void
       - setEnvironmentToStage(): void
       - setEnvironmentToProd(): void

       Must NOT contain: execute, executeWithConfig, execute2Step, execute2StepWithConfig, setLoggingEnabled, setFulfillmentAttributes, setSessionId, getSessionId.

       Keep the same type definitions (ColorMode, CacheConfig, RoktConfigType) and the TurboModuleRegistry.getEnforcing default export.

    2. **Rokt.tsx** (per REN-04, REN-05, REM-01, REM-02, REM-03, REM-04, SHP-04):
       Start from teammate's version which already has selectPlacements (with `identifier` parameter, not `viewName`) and removed deprecated methods. Then merge in our selectShoppableAds method:

       Final Rokt class should have EXACTLY these public static methods:
       - initialize(roktTagId, appVersion, fontFilesMap?)
       - selectPlacements(identifier, attributes, placeholders, roktConfig?)
       - selectShoppableAds(identifier, attributes, roktConfig?)
       - purchaseFinalized(placementId, catalogItemId, success)
       - setEnvironmentToStage()
       - setEnvironmentToProd()

       Must NOT contain: execute(), execute2Step(), setLoggingEnabled(), setFulfillmentAttributes(), setSessionId(), getSessionId().

       Keep the IRoktConfig, CacheConfig, RoktConfig, RoktConfigBuilder, ColorMode exports unchanged.
       Keep the architecture detection logic (isNewArchitecture) and module validation unchanged.
       Keep the JSDoc comment on selectShoppableAds from the current branch.

    3. Verify no references to `viewName` as a parameter name in either file (should be `identifier` per REN-05). The purchaseFinalized parameter can stay as `placementId` since that's the param name in the native SDK for that specific method.

  </action>
  <acceptance_criteria>
    - grep "selectPlacements" Rokt.Widget/src/NativeRoktWidget.ts returns matches
    - grep "selectShoppableAds" Rokt.Widget/src/NativeRoktWidget.ts returns matches
    - grep "execute(" Rokt.Widget/src/NativeRoktWidget.ts returns NO match
    - grep "execute2Step" Rokt.Widget/src/NativeRoktWidget.ts returns NO match
    - grep "setLoggingEnabled" Rokt.Widget/src/NativeRoktWidget.ts returns NO match
    - grep "setFulfillmentAttributes" Rokt.Widget/src/NativeRoktWidget.ts returns NO match
    - grep "setSessionId" Rokt.Widget/src/NativeRoktWidget.ts returns NO match
    - grep "getSessionId" Rokt.Widget/src/NativeRoktWidget.ts returns NO match
    - grep "selectPlacements" Rokt.Widget/src/Rokt.tsx returns matches
    - grep "selectShoppableAds" Rokt.Widget/src/Rokt.tsx returns matches
    - grep "execute(" Rokt.Widget/src/Rokt.tsx returns NO match (but executeWithConfig reference in comment is OK)
    - grep "setLoggingEnabled" Rokt.Widget/src/Rokt.tsx returns NO match
    - grep "setFulfillmentAttributes" Rokt.Widget/src/Rokt.tsx returns NO match
    - grep "setSessionId" Rokt.Widget/src/Rokt.tsx returns NO match
  </acceptance_criteria>
  <verify>
    <automated>cd Rokt.Widget && grep -q "selectPlacements" src/NativeRoktWidget.ts && grep -q "selectShoppableAds" src/NativeRoktWidget.ts && ! grep -q "execute(" src/NativeRoktWidget.ts && ! grep -q "setLoggingEnabled\|setFulfillmentAttributes\|setSessionId\|getSessionId\|execute2Step" src/NativeRoktWidget.ts && grep -q "selectPlacements" src/Rokt.tsx && grep -q "selectShoppableAds" src/Rokt.tsx && ! grep -q "setLoggingEnabled\|setFulfillmentAttributes\|execute2Step" src/Rokt.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>NativeRoktWidget.ts has clean v5.0.0 spec with selectPlacements + selectShoppableAds, all deprecated methods removed. Rokt.tsx has matching public API with identifier parameter naming.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary     | Description                                       |
| ------------ | ------------------------------------------------- |
| JS -> Native | Untrusted JS parameters cross to native SDK calls |

## STRIDE Threat Register

| Threat ID | Category            | Component                 | Disposition | Mitigation Plan                                                               |
| --------- | ------------------- | ------------------------- | ----------- | ----------------------------------------------------------------------------- |
| T-01-01   | T (Tampering)       | NativeRoktWidget.ts       | accept      | Parameters are passed through to native SDK which validates internally        |
| T-01-02   | I (Info Disclosure) | RNRoktWidgetModuleImpl.kt | mitigate    | logDebug only logs when debug flag is true, no sensitive data in event params |

</threat_model>

<verification>
1. NativeRoktWidget.ts has exactly 9 methods (initialize, initializeWithFontFiles, selectPlacements, selectPlacementsWithConfig, selectShoppableAds, selectShoppableAdsWithConfig, purchaseFinalized, setEnvironmentToStage, setEnvironmentToProd)
2. Rokt.tsx has exactly 6 public static methods (initialize, selectPlacements, selectShoppableAds, purchaseFinalized, setEnvironmentToStage, setEnvironmentToProd)
3. Android build.gradle has roktsdk:5.0.0
4. RNRoktWidgetModuleImpl.kt has no deprecated method references and handles new shoppable ads events
5. No references to `viewName` as parameter in TS files (uses `identifier`)
</verification>

<success_criteria>

- TypeScript compilation: `cd Rokt.Widget && npx tsc --noEmit` passes
- All deprecated methods removed from TS and Android impl
- Android SDK version is 5.0.0
- Shoppable ads methods present in TurboModule spec and public API
- New event types (CartItemInstantPurchaseInitiated, CartItemInstantPurchaseFailure, CartItemDevicePay, InstantPurchaseDismissal) handled in Android event listener
  </success_criteria>

<output>
After completion, create `.planning/phases/01-sdk-bridge-events/01-01-SUMMARY.md`
</output>
