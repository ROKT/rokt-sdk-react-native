---
phase: 01-sdk-bridge-events
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt
  - Rokt.Widget/android/src/newarch/java/com/rokt/reactnativesdk/RNRoktWidgetModule.kt
  - Rokt.Widget/android/src/oldarch/java/com/rokt/reactnativesdk/RNRoktWidgetModule.kt
  - Rokt.Widget/src/index.tsx
autonomous: false
requirements:
  - EVT-02
  - EVT-03
  - SHP-03
  - REN-04

must_haves:
  truths:
    - "Android event listener handles new shoppable ads events (CartItemInstantPurchaseInitiated, CartItemInstantPurchaseFailure, InstantPurchaseDismissal, CartItemDevicePay)"
    - "Android selectShoppableAds is wired through both arch wrappers to the native SDK"
    - "Public index.tsx exports are clean (no deprecated method references)"
    - "TypeScript compiles with no errors"
    - "Both platforms have consistent event names for all shoppable ads events"
  artifacts:
    - path: "Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt"
      provides: "Android event listener with all shoppable ads event types"
      contains: "CartItemInstantPurchaseInitiated"
    - path: "Rokt.Widget/src/index.tsx"
      provides: "Clean barrel exports"
  key_links:
    - from: "Rokt.Widget/android/src/newarch/java/com/rokt/reactnativesdk/RNRoktWidgetModule.kt"
      to: "Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt"
      via: "impl.selectShoppableAds delegation"
      pattern: "impl\\.selectShoppableAds"
---

<objective>
Add missing shoppable ads event types to Android event listener, verify the end-to-end wiring for selectShoppableAds on Android, ensure public exports are clean, and do a final cross-platform consistency check.

Purpose: Plan 01 set up the Android bridge with selectPlacements/selectShoppableAds and cleaned deprecated methods. Plan 02 did the same for iOS. This plan ensures the Android event listener handles all the new shoppable ads event types (matching iOS), verifies the index.tsx barrel exports are clean, and does a final TypeScript compilation check.

Output: Complete, consistent Android event handling and verified clean SDK surface across both platforms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sdk-bridge-events/01-CONTEXT.md
@.planning/phases/01-sdk-bridge-events/01-01-SUMMARY.md
@.planning/phases/01-sdk-bridge-events/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shoppable ads event types to Android and verify barrel exports</name>
  <files>Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt, Rokt.Widget/src/index.tsx</files>
  <read_first>
    - Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt (post plan-01 state)
    - Rokt.Widget/android/src/newarch/java/com/rokt/reactnativesdk/RNRoktWidgetModule.kt (post plan-01 state)
    - Rokt.Widget/android/src/oldarch/java/com/rokt/reactnativesdk/RNRoktWidgetModule.kt (post plan-01 state)
    - Rokt.Widget/src/index.tsx (current state)
    - Rokt.Widget/src/Rokt.tsx (post plan-01 state, to confirm exports)
    - Rokt.Widget/ios/RoktEventManager.m (post plan-02 state, for event name consistency)
  </read_first>
  <action>
**RNRoktWidgetModuleImpl.kt** — In the `startRoktEventListener` method, add handling for the new shoppable ads event types that were added to iOS in plan 02. These should be added as additional `when` branches in the `flow.collect` block, after the existing `CartItemInstantPurchase` branch and before the `else` branch:

```kotlin
is RoktEvent.CartItemInstantPurchaseInitiated -> {
    eventName = "CartItemInstantPurchaseInitiated"
    params.putString("catalogItemId", event.catalogItemId)
    params.putString("cartItemId", event.cartItemId)
    event.id
}

is RoktEvent.CartItemInstantPurchaseFailure -> {
    eventName = "CartItemInstantPurchaseFailure"
    params.putString("catalogItemId", event.catalogItemId)
    // error info if available on the event
    event.id
}

is RoktEvent.InstantPurchaseDismissal -> {
    eventName = "InstantPurchaseDismissal"
    event.id
}

is RoktEvent.CartItemDevicePay -> {
    eventName = "CartItemDevicePay"
    // paymentProvider if available on the event
    event.id
}
```

IMPORTANT: The exact class names and properties depend on the Android SDK 5.0.0 API. Check what's available in the `com.rokt.roktsdk.RoktEvent` sealed class. If any event type is not present in the Android SDK (some events may be iOS-only), skip it — the `else -> "Unknown"` branch will handle it gracefully. Do NOT add imports or branches for classes that don't exist.

Ensure the `event.id` property is used (SDK 5.0 renamed this from `event.identifier` — check the actual property name on the sealed class).

**index.tsx** — Read the current barrel file and verify it exports only the correct symbols. The file should export:

- `Rokt` (the class)
- `RoktEmbeddedView` (the component)
- `RoktEventManager` (native module reference)
- `RoktConfigBuilder` (builder class)
- `CacheConfig` (config class)
- Type exports: `IRoktConfig`, `ColorMode`

If the index.tsx references any deprecated methods or types (e.g., exports related to `execute2Step`, `FulfillmentAttributes`, `SessionId`), remove those references. If it's already clean, leave it unchanged.

**Verify arch wrapper wiring** — Confirm that both `newarch/RNRoktWidgetModule.kt` and `oldarch/RNRoktWidgetModule.kt` properly delegate `selectShoppableAds` and `selectShoppableAdsWithConfig` to `impl.selectShoppableAds(...)`. If plan 01 already did this correctly, just verify — no changes needed.

**TypeScript compilation** — Run `cd Rokt.Widget && npx tsc --noEmit` to verify the entire TypeScript layer compiles without errors after all changes from plans 01 and 02.
</action>
<acceptance_criteria> - RNRoktWidgetModuleImpl.kt event listener contains `CartItemInstantPurchaseInitiated` branch (or documents it doesn't exist in Android SDK) - RNRoktWidgetModuleImpl.kt event listener contains `InstantPurchaseDismissal` branch (or documents it doesn't exist in Android SDK) - RNRoktWidgetModuleImpl.kt event listener contains `CartItemDevicePay` branch (or documents it doesn't exist in Android SDK) - index.tsx exports Rokt, RoktEmbeddedView, RoktEventManager, RoktConfigBuilder, CacheConfig, IRoktConfig, ColorMode - index.tsx does NOT export any deprecated symbols - `cd Rokt.Widget && npx tsc --noEmit` exits with code 0 - Event names are consistent between iOS (RoktEventManager.m) and Android (RNRoktWidgetModuleImpl.kt)
</acceptance_criteria>
<verify>
<automated>cd /Users/thomson/conductor/workspaces/rokt-sdk-react-native/nagoya && grep -c "CartItemInstantPurchase" Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt && grep -q "Rokt" Rokt.Widget/src/index.tsx && echo "PASS"</automated>
</verify>
<done>
Android event listener handles all shoppable ads events consistently with iOS. Barrel exports are clean. TypeScript compiles.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete SDK surface is clean across all layers</name>
  <files>Rokt.Widget/src/NativeRoktWidget.ts, Rokt.Widget/src/Rokt.tsx, Rokt.Widget/ios/RNRoktWidget.mm, Rokt.Widget/android/src/main/java/com/rokt/reactnativesdk/RNRoktWidgetModuleImpl.kt</files>
  <action>
    This is a human verification checkpoint. Claude will run the grep checks below automatically before presenting results to the user for final approval.

    Run these automated checks:
    1. `grep -rn "execute2Step\|setLoggingEnabled\|setFulfillmentAttributes\|setSessionId\|getSessionId" Rokt.Widget/src/ Rokt.Widget/ios/ Rokt.Widget/android/src/` — expect NO output
    2. `grep -rn "selectPlacements" Rokt.Widget/src/ Rokt.Widget/ios/RNRoktWidget.mm Rokt.Widget/android/src/` — expect multiple hits
    3. `grep -rn "selectShoppableAds" Rokt.Widget/src/ Rokt.Widget/ios/RNRoktWidget.mm Rokt.Widget/android/src/` — expect multiple hits
    4. `grep "roktsdk" Rokt.Widget/android/build.gradle` — expect `roktsdk:5.0.0`
    5. `cd Rokt.Widget && npx tsc --noEmit` — expect no errors

    Present the results to the user for final approval.

  </action>
  <what-built>
    Complete v5.0.0 migration of the React Native SDK bridge layer:
    - TypeScript: selectPlacements + selectShoppableAds in TurboModule spec and public API
    - iOS: selectPlacements with onEvent pattern + selectShoppableAds in bridge, all events handled in event manager
    - Android: selectPlacements + selectShoppableAds in impl and both arch wrappers, all events in event listener
    - All deprecated methods removed (execute, execute2Step, setLoggingEnabled, setFulfillmentAttributes, setSessionId, getSessionId)
    - Android SDK bumped to 5.0.0, iOS already at 5.0.0
  </what-built>
  <how-to-verify>
    Run these grep checks to confirm no deprecated methods remain anywhere:

    1. Check NO deprecated methods in any source file:
       ```
       grep -rn "execute2Step\|setLoggingEnabled\|setFulfillmentAttributes\|setSessionId\|getSessionId" Rokt.Widget/src/ Rokt.Widget/ios/ Rokt.Widget/android/src/
       ```
       Expected: NO output (all deprecated methods removed)

    2. Check selectPlacements exists in all layers:
       ```
       grep -rn "selectPlacements" Rokt.Widget/src/ Rokt.Widget/ios/RNRoktWidget.mm Rokt.Widget/android/src/
       ```
       Expected: Multiple hits across TS, iOS, Android

    3. Check selectShoppableAds exists in all layers:
       ```
       grep -rn "selectShoppableAds" Rokt.Widget/src/ Rokt.Widget/ios/RNRoktWidget.mm Rokt.Widget/android/src/
       ```
       Expected: Multiple hits across TS, iOS, Android

    4. Check Android SDK version:
       ```
       grep "roktsdk" Rokt.Widget/android/build.gradle
       ```
       Expected: `roktsdk:5.0.0`

    5. TypeScript builds:
       ```
       cd Rokt.Widget && npx tsc --noEmit
       ```
       Expected: No errors

  </how-to-verify>
  <verify>
    <automated>cd /Users/thomson/conductor/workspaces/rokt-sdk-react-native/nagoya && ! grep -rq "execute2Step\|setLoggingEnabled\|setFulfillmentAttributes\|setSessionId\|getSessionId" Rokt.Widget/src/ Rokt.Widget/ios/ Rokt.Widget/android/src/ && grep -rq "selectPlacements" Rokt.Widget/src/ && grep -rq "selectShoppableAds" Rokt.Widget/src/ && echo "PASS"</automated>
  </verify>
  <done>Human confirms all 5 verification checks pass. No deprecated methods remain. Both platforms have consistent, complete v5.0.0 API surface.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                       |
| ------------------------------ | ----------------------------------------------------------------- |
| Native SDK -> JS event emitter | Purchase events containing financial data cross from native to JS |

## STRIDE Threat Register

| Threat ID | Category              | Component                      | Disposition | Mitigation Plan                                                                                 |
| --------- | --------------------- | ------------------------------ | ----------- | ----------------------------------------------------------------------------------------------- |
| T-01-01   | I (Info Disclosure)   | CartItemInstantPurchase events | accept      | Purchase data is intended for the consuming app; same-process bridge                            |
| T-01-02   | D (Denial of Service) | Event listener flow collection | accept      | Lifecycle-scoped collection prevents runaway listeners; existing pattern from teammate's branch |

</threat_model>

<verification>
1. Android event listener handles all new shoppable ads event types
2. Event names are consistent between iOS and Android
3. No deprecated methods remain in any layer
4. TypeScript compiles cleanly
5. Human verification confirms all checks pass
</verification>

<success_criteria>

- All 21 phase 1 requirement IDs are addressed across the 3 plans
- Both platforms have complete, consistent API surfaces for v5.0.0
- No deprecated methods remain anywhere
- TypeScript compilation succeeds
- Human verification passes
  </success_criteria>

<output>
After completion, create `.planning/phases/01-sdk-bridge-events/01-03-SUMMARY.md`
</output>
